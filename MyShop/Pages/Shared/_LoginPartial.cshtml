@using Microsoft.AspNetCore.Identity
@using MyShop.Entities;

@inject SignInManager<ShopAppWebUser> SignInManager
@inject UserManager<ShopAppWebUser> UserManager

<ul class="navbar-nav">
@if (SignInManager.IsSignedIn(User))
{
}
else
{
}
</ul>
